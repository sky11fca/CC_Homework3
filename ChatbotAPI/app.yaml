service: chatbotapi
runtime: custom
env: flex

env_variables:
  # Replace this with your real model endpoint (for example Vertex AI proxy endpoint).
  OLLAMA_HOST: http://127.0.0.1:11434

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 2
  cpu_utilization:
    target_utilization: 0.6